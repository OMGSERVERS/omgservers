package com.omgservers.service.module.dispatcher.impl.service.roomService.dto;

public enum MessageEncodingEnum {
    /**
     * It's a base64 encoded binary message.
     */
    B64,

    /**
     * It's a plain text message.
     */
    TXT
}
