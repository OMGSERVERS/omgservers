package com.omgservers.service.module.dispatcher.impl.service.dispatcherService.component;

public enum DispatcherConnectionTypeEnum {
    /**
     * Connection was routed to another server.
     */
    ROUTED,

    /**
     * Connection is handled by this server.
     */
    SERVER,
}
