package com.omgservers.model.runtime;

public enum RuntimeQualifierEnum {

    /**
     * It's runtime for the lobby.
     */
    LOBBY,

    /**
     * It's runtime for the match.
     */
    MATCH,
}
