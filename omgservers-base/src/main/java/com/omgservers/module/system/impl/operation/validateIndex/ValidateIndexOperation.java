package com.omgservers.module.system.impl.operation.validateIndex;

import com.omgservers.model.index.IndexModel;

public interface ValidateIndexOperation {
    IndexModel validateIndex(IndexModel indexModel);
}
